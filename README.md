# multithreaded-progress-bar
Multithreaded Progress Bar (Windows, C)  A Windows console application written in C that demonstrates **multithreading with synchronization** by displaying four animated progress bars running concurrently.  Each progress bar runs in its own thread and updates the console safely using a **Critical Section** to prevent race conditions.
